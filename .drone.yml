---
kind: pipeline
name: docker

platform:
  os: linux
  arch: amd64

steps:
- name: build
  pull: always
  image: spritsail/docker-build
  settings:
    repo: frebib/salt-master

- name: test
  pull: always
  image: spritsail/docker-test
  settings:
    run: |
      salt-master --versions-report

- name: publish
  pull: always
  image: spritsail/docker-publish
  settings:
    repo: frebib/salt-master
    tags:
    - latest
    - "%label io.spritsail.version.salt"
    username: {from_secret: docker_username}
    password: {from_secret: docker_password}
  when:
    branch:
    - master
    event:
    - push

---
kind: pipeline
name: update-readme

steps:
- name: dockerhub-readme
  pull: always
  image: jlesage/drone-push-readme
  settings:
    username: {from_secret: docker_username}
    password: {from_secret: docker_password}
    repo: frebib/salt-master
  when:
    branch:
    - master
    event:
    - push

---
kind: signature
hmac: 3ebb146a100834daf9568998b1f81a1ef3a60acba36671ac6c2162315e5a0cab

...
